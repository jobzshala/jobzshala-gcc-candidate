import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register | Jobzshala",
  description: "Create your verified GCC Workforce Profile and get discovered by verified employers across the GCC.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
